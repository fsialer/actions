name: Hola mundo

on: [push]

jobs:
  hola:
    runs-on: ubuntu-latest
    steps:
      - name: hola mundo
        run: echo "Hola mundo"
  other:
    runs-on: ubuntu-latest
    steps:
      - name: LS
        run: ls -al

